# Urban tree analysis

This example project explores whether tree-canopy coverage differs across urban
neighbourhoods. It exists only as a safe fixture for testing repo-reviewer.

The analysis code is somewhere under `src/`.
